# Clé API PRIM (Île-de-France Mobilités)
# Créez un compte sur https://prim.iledefrance-mobilites.fr puis générez un jeton
# sous "Mon compte → Mes jetons d'authentification".
PRIM_API_KEY=votre_jeton_prim_ici

# Optionnel : port d'écoute HTTP (Render fournit automatiquement $PORT).
# PORT=8000
